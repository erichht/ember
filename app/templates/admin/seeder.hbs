<h1>Seeder, our Data Center</h1>

<div class="row">
    <div class="col-mid-4">{{number-box title="Libraries" number=libraries.length}}</div>
    <div class="col-mid-4">{{number-box title="Authors" number=authors.length}}</div>
    <div class="col-mid-4">{{number-box title="Books" number=books.length}}</div>
</div>